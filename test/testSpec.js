define([ "device" ], function (d) {
	describe("device", function () {

		var device;
		before(function(){
			device = d.newDevice(navigator, window);
			console.log(device);
		});

		after(function(){

		});

		it("", function(){

		});

	});
});
